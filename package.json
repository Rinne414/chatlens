{
  "name": "qqnt-readonly-summary-toolkit",
  "version": "0.0.13",
  "description": "Read-only QQNT group chat briefing: background refresh, AI summaries, local web console (Windows and Linux).",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/launcher.js",
    "control-center": "node src/server/control_center.js",
    "summarize": "node src/pipeline/summary_run.js",
    "refresh": "node src/pipeline/refresh_run.js",
    "list-groups": "node src/pipeline/group_list_run.js",
    "harvest-existing": "node src/harvest_existing_runs.js",
    "icons": "node scripts/make_icons.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "better-sqlite3-multiple-ciphers": "^12.11.1"
  }
}
